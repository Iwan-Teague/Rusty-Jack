# Daemon tests (moved into tests/daemon)

This file documents the daemon tests and expected behavior. It has been moved into `tests/daemon` for organization.

See `run_daemon_tests.sh` and `run_daemon_tests.ps1` for execution instructions.
