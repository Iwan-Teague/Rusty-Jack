# Rustyjack WiFi Hotplug Rules
# Place this file in /etc/udev/rules.d/99-rustyjack-wifi.rules

# USB WiFi adapter insertion
ACTION=="add", SUBSYSTEM=="net", KERNEL=="wlan*", RUN+="/opt/rustyjack/scripts/wifi_hotplug.sh add %k"

# USB WiFi adapter removal  
ACTION=="remove", SUBSYSTEM=="net", KERNEL=="wlan*", RUN+="/opt/rustyjack/scripts/wifi_hotplug.sh remove %k"

# Reload udev rules with: sudo udevadm control --reload-rules && sudo udevadm trigger
